<Page x:Class="TicketSystem.Request"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:TicketSystem"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Request">

    <Grid Background="Aqua">
        <DockPanel Grid.ColumnSpan="2">
            <StackPanel DockPanel.Dock="Top" Orientation="Horizontal"  Height="47">
                <Button Content="Создать запрос" Width="220" FontSize="20" />
                <Button Content="Мои запросы" Width="207" FontSize="20" />
            </StackPanel>
            <Frame x:Name="RequestFrame" NavigationUIVisibility="Hidden" />
        </DockPanel>
    </Grid>
</Page>
